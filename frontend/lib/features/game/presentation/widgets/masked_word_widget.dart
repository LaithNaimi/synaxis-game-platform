import 'package:flutter/material.dart';

class MaskedWordWidget extends StatelessWidget {
  const MaskedWordWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
