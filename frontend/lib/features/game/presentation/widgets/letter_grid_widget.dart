import 'package:flutter/material.dart';

class LetterGridWidget extends StatelessWidget {
  const LetterGridWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
