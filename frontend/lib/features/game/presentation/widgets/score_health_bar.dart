import 'package:flutter/material.dart';

class ScoreHealthBar extends StatelessWidget {
  const ScoreHealthBar({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
