import 'package:flutter/material.dart';

class RoomSettingsCard extends StatelessWidget {
  const RoomSettingsCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
