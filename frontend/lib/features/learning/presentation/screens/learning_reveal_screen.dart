import 'package:flutter/material.dart';

class LearningRevealScreen extends StatelessWidget {
  const LearningRevealScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Learning')));
  }
}
